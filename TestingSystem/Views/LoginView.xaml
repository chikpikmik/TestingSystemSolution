<Page x:Class="TestingSystem.Views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TestingSystem.Views"
      xmlns:vm="clr-namespace:TestingSystem.ViewModels"
      xmlns:utils="clr-namespace:TestingSystem.Utils"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="272.222"
      Title="Login">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="13*"/>
            <ColumnDefinition Width="147*"/>
        </Grid.ColumnDefinitions>
        <Button Content="Войти" Command="{Binding LoginCommand}" Margin="62,238,0,0" VerticalAlignment="Top" Height="36" Width="116" HorizontalAlignment="Left" Grid.Column="1"/>
        <TextBlock Text="Неверно! Повторите попытку." Visibility="Hidden" Foreground="Red" HorizontalAlignment="Left" Margin="50,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center" Grid.Column="1" Height="16" Width="166" />
        <Button Content="Регистрация" Command="{Binding NavigateToRegisterViewCommand}" Margin="62,312,0,0" VerticalAlignment="Top" Height="21" Width="116" HorizontalAlignment="Left" Grid.Column="1"/>
        <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Margin="62,141,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="120" HorizontalAlignment="Left" Grid.Column="1" Height="18"/>
        <PasswordBox local:PasswordBoxBinding.Password="{Binding Password, Mode=TwoWay}" Margin="62,177,0,0" VerticalAlignment="Top" Width="120" HorizontalAlignment="Left" Grid.Column="1" Height="18"/>
        <Label Content="Login:" Margin="20,137,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Grid.Column="1" Height="26" Width="42"/>
        <Label Content="Password:" Margin="0,173,0,0" VerticalAlignment="Top" HorizontalAlignment="Left" Grid.Column="1" Height="26" Width="62"/>
    </Grid>
</Page>